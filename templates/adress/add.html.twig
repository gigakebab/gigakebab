{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    <h1>Ajouter une nouvelle adresse</h1>

    <h2>Vos adresses existantes :</h2>

    {% for address in addresses %}
        <a href="#"><h3>{{ address.address }}</h3></a>
    {% endfor %}

    {{ form(form) }}

{% endblock %}